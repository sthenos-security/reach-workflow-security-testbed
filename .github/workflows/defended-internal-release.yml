name: Defended - internal release

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  internal-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3df4ab11eba7bda6032a0b82a6bb43b11571feac
      - name: Safe internal status
        run: |
          echo "internal release checks only"
