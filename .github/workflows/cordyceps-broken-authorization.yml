name: Fixture - Cordyceps broken authorization

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  id-token: write

jobs:
  missing-authorization-gate:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Simulated privileged packaging step
        run: |
          echo "simulated npm publish for ${{ github.event.pull_request.head.ref }}"
