name: Fixture - reusable inherited secrets

on:
  workflow_call:
    secrets:
      RELEASE_TOKEN:
        required: false

permissions:
  contents: write

jobs:
  publish-from-reusable:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Simulated inherited-secret publish
        run: |
          echo "simulated deploy using inherited release authority"
