name: oidc trust exposure

on:
  issue_comment:
    types: [created]

permissions:
  id-token: write
  contents: read

jobs:
  cloud:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: aws sts get-caller-identity
